# 系统架构说明

## 逻辑分层

- 客户端层：Electron 主进程、Preload 桥接、Vue 渲染层
- 应用层：生成服务、审查服务、知识服务、模型配置服务、手册服务
- 领域层：模型网关、规则引擎、生成编排器、审查编排器、一致性检查器、手册生成器
- 基础设施层：本地模型、数据库、对象存储、向量库、日志与审计

## 关键链路

- 生成链路：模板选择 -> 知识检索 -> 草稿生成 -> 自动审查
- 审查链路：文档输入 -> 结构解析 -> 规则审查 -> 语义检查 -> 问题聚合
- 手册链路：Electron 截图 -> 元数据回传 -> 手册段落生成 -> 导出
